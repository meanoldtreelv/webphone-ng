import { RootState } from "./../../redux/store";

export const sidecarSidebar = (state: RootState) => state.sidecar.sidecarSidebar;
export const tabSelected = (state: RootState) => state.sidecar.tabSelected;
